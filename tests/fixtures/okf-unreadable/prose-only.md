# Prose-only concept file (fixture)

No frontmatter at all — an ordinary concept file that declares no state space.
This must **not** flag: telling it apart from a file that meant to declare one
needs the OKF format to settle whether frontmatter is mandatory (issue #20).

## An anchor {#prose-anchor}

Prose the norm files cite.
